<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/settingsRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/settingsPageBg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="8dp"
        android:paddingEnd="16dp">

        <ImageButton
            android:id="@+id/settingsBack"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            android:contentDescription="Close" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="Settings"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="?android:attr/textColorPrimary" />
    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:paddingTop="12dp"
            android:paddingBottom="28dp">

            <!-- Search engine -->
            <TextView style="@style/SettingsCardLabel" android:text="Search engine" />
            <LinearLayout style="@style/SettingsCard">
                <RadioGroup
                    android:id="@+id/engineGroup"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:paddingStart="18dp"
                    android:paddingEnd="18dp"
                    android:paddingTop="6dp"
                    android:paddingBottom="6dp" />
            </LinearLayout>

            <!-- Browsing -->
            <TextView style="@style/SettingsCardLabel" android:text="Browsing" />
            <LinearLayout style="@style/SettingsCard">
                <TextView android:id="@+id/rowSecurity" style="@style/SettingsRow"
                    android:text="Search Security" android:drawableStart="@drawable/set_security" />
                <TextView android:id="@+id/rowAdblock" style="@style/SettingsRow"
                    android:text="Ad blocking" android:drawableStart="@drawable/set_adblock" />
                <TextView android:id="@+id/rowSiteSettings" style="@style/SettingsRow"
                    android:text="Site settings" android:drawableStart="@drawable/set_site" />
                <TextView android:id="@+id/rowAccessibility" style="@style/SettingsRow"
                    android:text="Accessibility" android:drawableStart="@drawable/set_accessibility" />
                <TextView android:id="@+id/rowCustomize" style="@style/SettingsRow"
                    android:text="Customize your Search" android:drawableStart="@drawable/set_customize" />
            </LinearLayout>

            <!-- Appearance -->
            <TextView style="@style/SettingsCardLabel" android:text="Appearance" />
            <LinearLayout style="@style/SettingsCard">
                <RadioGroup
                    android:id="@+id/themeGroup"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:paddingStart="18dp"
                    android:paddingEnd="18dp"
                    android:paddingTop="6dp"
                    android:paddingBottom="6dp" />
            </LinearLayout>

            <!-- Data -->
            <TextView style="@style/SettingsCardLabel" android:text="Data" />
            <LinearLayout style="@style/SettingsCard">
                <TextView android:id="@+id/clearData" style="@style/SettingsRow"
                    android:text="Clear browsing data" android:textColor="#E53935"
                    android:drawableStart="@drawable/set_clear" />
            </LinearLayout>

            <!-- About -->
            <TextView style="@style/SettingsCardLabel" android:text="About" />
            <LinearLayout style="@style/SettingsCard">
                <TextView
                    android:id="@+id/aboutText"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:paddingStart="18dp"
                    android:paddingEnd="18dp"
                    android:paddingTop="16dp"
                    android:paddingBottom="16dp"
                    android:text="Search Browser"
                    android:textSize="15sp"
                    android:textColor="?android:attr/textColorPrimary" />
                <TextView android:id="@+id/rowCheckUpdate" style="@style/SettingsRow"
                    android:text="Check for updates" android:drawableStart="@drawable/set_update" />
                <TextView android:id="@+id/rowPrivacy" style="@style/SettingsRow"
                    android:text="Privacy Policy" android:drawableStart="@drawable/set_privacy" />
                <TextView android:id="@+id/rowTerms" style="@style/SettingsRow"
                    android:text="Terms of Use" android:drawableStart="@drawable/set_terms" />
            </LinearLayout>

        </LinearLayout>
    </ScrollView>
</LinearLayout>
